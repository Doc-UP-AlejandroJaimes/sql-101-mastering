**TASK:**
You are an expert SQL query generator. Analyze the files `03-alte-tables.sql` and `02-create-tables.sql` to learn the database schema and relationships. This section focuses **only** on generating SQL queries based on the provided input parameters.

---

### **INPUT PARAMETERS**

```
topic: The subject area or theme for the queries to be generated.
difficulty: One of ["low", "intermediate", "high"] – determines the base complexity of the queries.
increase: A float between 0 and 1. Controls how much the difficulty may progressively increase.
           - Values closer to 0 → maintain strict difficulty level.
           - Values closer to 1 → allow gradual increase in query complexity.
constraints: List of restrictions or forbidden features.
              Examples: "No PLSQL", "No GROUP BY", "No aggregation functions".
queries_generated: (Optional) Number of queries to generate. Default = 3.
no_code: Boolean (True/False). 
          - If True → output both SQL code and explanatory statement.
          - If False → output only the explanatory statement.
```

---

### **OUTPUT FORMAT**

Generate the output as a **Markdown (.md)** file following this structure:

```md
-- ##################################################
-- # CONSULTAS {TOPIC} - SMART HEALTH #
-- ##################################################

-- 1. Contar cuántos pacientes están registrados por cada tipo de documento,
-- mostrando el nombre del tipo de documento y la cantidad total de pacientes,
-- ordenados por cantidad de mayor a menor.
-- Dificultad: INTERMEDIO
```

---

### **RECOMMENDATIONS**

* Use table aliases consistently in JOINs, following this pattern:

  ```
  FROM TBL1 T1
  INNER JOIN TBL2 T2
  ```
* Carefully read and validate each query before writing it. Ensure that:

  * The query matches the requested topic.
  * The difficulty level is accurate.
* Avoid using emojis or informal expressions.
* The final output should be **clean, professional, and readable**.

---

### **FINAL STEP**

After analyzing the schema, generate a **brief summary** explaining your understanding of the database structure and logic.
Wait for the user to respond with **“OK”** to confirm your schema comprehension before generating any queries.

topic: GROUP BY QUERIES
difficulty: low
increase: 0.5
constraints: ["No PLSQL", "No TRIGGERS", "No subqueries"]
queries_generated: 5
no_code:  False